#include <iostream>
using namespace std;
int main(){

int vidas; //10, 25 nuneros inteiros 
char letra; // 'B', letras com apostrofo 
double decimal;// 2.5, numeors decimais precisos
float decimal;//numeos decimais não tão precisos
bool vivo;// variavel definida com verddeira ou falsa(true or false)

    return 0;
}
